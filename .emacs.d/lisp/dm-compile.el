;;; dm-compile -- Settings of compilation mode

;;; Commentary:

;;; Code:

(require 'compile)

(setq compilation-ask-about-save nil
      compilation-scroll-output 'first-error)

(provide 'dm-compile)
;;; dm-compile.el ends here
